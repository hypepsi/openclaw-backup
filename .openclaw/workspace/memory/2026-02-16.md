# 2026-02-16

- User reported missed market brief delivery; root cause was cron `delivery.mode=none` from prior anti-duplicate change.
- Fixed schedule delivery to `announce` for both jobs:
  - 16:00 Hang Seng Tech report
  - 18:00 Gold + BTC report
  and user confirmed they want these delivered.
- During Passwall2 troubleshooting, adding domains with `add_list` corrupted `passwall2.Direct.domain_list` format, which broke shunt startup (`util_xray.lua ... gsub string expected, got table`) and caused network drop when switching to shunt.
- Restored and repaired Passwall2 config by resetting `Direct.domain_list` to clean string-list format; shunt mode (`myshunt`) could start again.
- User preference reinforced: when diagnosing network issues, prioritize read-only checks and avoid unsolicited config changes.
- Latest read-only check for WeChat lag: router-side DNS/ICMP to common WeChat image domains looked healthy (low latency, no packet loss); suggested live hit-tracing during reproduction without changing config.
